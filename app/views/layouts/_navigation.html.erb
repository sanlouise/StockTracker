
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "StockTracker", root_path, class:"navbar-brand", id: "logo" %>
       <% if current_user %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        
        
          <li><%= link_to "My Portfolio", my_portfolio_path(current_user) %></li>
          <li><%= link_to "My Profile", edit_registration_path(current_user)  %></li>
          <li><%= link_to "Friends", my_friends_path %></li>
        
          <ul class="nav navbar-right col-md-4">
          <li class="col-md-8 user-name text-right"> 
            <i class = "fa fa-user">
              <%= current_user.full_name.present? ? current_user.full_name : current_user.email %>
            </i>
            <li class="col-md-4 logout"><%= link_to "Logout", destroy_user_session_path,
            class: "btn btn-xs btn-danger", method: :delete %> </li>
          </li>
        </ul>
      </ul>
      <ul class="nav navbar-nav navbar-right">

          
        <% else %>
        
          <li><%= link_to 'Login', login_path %> </li>
          <li><%= link_to 'Signup', signup_path %> </li>
          
    <% end %>
        
      </ul>
    </div>
  </div><!-- /.container-fluid -->
</nav>